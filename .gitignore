# macOS
.DS_Store
.AppleDouble
.LSOverride

# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Editor
*~
*.swp
*.swo
.vscode/

# Stow (but track .stow-local-ignore files)
.stow-*
!.stow-local-ignore

# Neovim - embedded git repository (managed separately)
nvim/.config/nvim/.git/
nvim/.config/nvim/.gitignore

# Tmux plugins - managed by TPM (not tracked)
tmux/.config/tmux/plugins/
